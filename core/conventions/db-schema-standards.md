# 🧾 Database Schema Standards (2BSimpleCore)

## 🎯 Objective
กำหนดมาตรฐานในการออกแบบและพัฒนาโครงสร้างฐานข้อมูล (Database Schema)  
เพื่อให้ Claude Agents และทีมพัฒนาใช้รูปแบบเดียวกันในทุก Feature  
รองรับการตรวจสอบอัตโนมัติ และการขยายระบบในอนาคต

---

## 🧱 Primary Keys
- ทุกตารางต้องมีฟิลด์ `id` เป็น Primary Key  
- ตั้งชื่อเป็น `id` เท่านั้น ห้ามใช้ชื่ออื่น  
- Type: `SERIAL` หรือ `BIGSERIAL` (ขึ้นอยู่กับขนาดข้อมูล)
- ห้ามใช้ composite key ยกเว้นกรณีตาราง pivot หรือ join table

```sql
CREATE TABLE products (
  id SERIAL PRIMARY KEY,
  name VARCHAR(255) NOT NULL
);
🕒 Timestamp Fields
- ทุกตารางต้องมีฟิลด์เวลามาตรฐาน 3 ฟิลด์:
- Field	Type	Description
  created_at	TIMESTAMPTZ DEFAULT NOW()	วันที่สร้างข้อมูล
  updated_at	TIMESTAMPTZ DEFAULT NOW()	วันที่แก้ไขข้อมูลล่าสุด
  deleted_at	TIMESTAMPTZ NULL	สำหรับ soft delete

🔗 Foreign Keys
🧠 หลักการตั้งชื่อ
- ใช้รูปแบบ {table}_id
- ตั้งชื่อ constraint เป็น fk_<child>_<parent>
- ต้องระบุการกระทำเมื่อ parent ถูกลบ (ON DELETE)

⚙️ Indexing
- ต้องสร้าง index สำหรับ field ที่ใช้ใน WHERE, JOIN, หรือ ORDER BY
- ใช้รูปแบบชื่อ: idx_<table>_<column>
- หลีกเลี่ยงการสร้าง index ซ้ำซ้อน

🧩 Soft Delete
- ใช้ฟิลด์ deleted_at TIMESTAMP NULL
- Query ต้องกรองด้วยเงื่อนไข WHERE deleted_at IS NULL
- ใช้สำหรับระบบที่ต้องการเก็บประวัติ เช่น Product, Employee, Document

✅ Data Integrity
- ทุกตารางต้องมี Foreign Key ที่เชื่อมโยงกันอย่างชัดเจน
- ทุก ENUM ควรเก็บใน lookup table หรือใช้ constraint ตรวจสอบค่า
- ห้ามใช้ TEXT สำหรับข้อมูลที่มีค่า fix เช่น status, type (ควรใช้ ENUM หรือ FK)
- ใช้ CHECK() constraint เพื่อจำกัดค่าที่อนุญาต

📘 Migration File Naming
- ใช้รูปแบบมาตรฐานเดียวกันทั่วระบบ: YYYYMMDD_create_<entity>_table.sql

  ✅ แนวทาง
  - ใช้วันที่ (YYYYMMDD) เพื่อเรียงลำดับ migration
  - คำว่า create สำหรับสร้างตารางใหม่
  - คำว่า alter สำหรับการปรับปรุง schema เดิม

🧪 Validation Rule
  ประเภท	ตัวอย่าง	กฎ
  จำนวนเงิน	DECIMAL(12,2)	ต้องไม่ติดลบ
  วันที่	DATE	ห้ามเป็นอดีต (ถ้าเป็น planned date)
  สถานะ	ENUM หรือ FK	ต้องตรงกับ lookup table
  Boolean	BOOLEAN	ใช้ชื่อฟิลด์ขึ้นต้นด้วย is_ เช่น is_active

🔁 Relation Design
- ใช้ความสัมพันธ์แบบ 1:N หรือ N:M ตาม business logic
- ตารางกลาง (junction table) ต้องใช้ชื่อแบบรวม เช่น: product_suppliers, employee_departments
- ทุก junction table ต้องมี PK หรือ composite key คู่

🧩 Trigger & Function (ถ้ามี)
- Trigger ต้องตั้งชื่อ: trg_<table>_<action>
- Function ต้องตั้งชื่อ: fn_<description>
- ใช้ trigger สำหรับอัปเดต updated_at อัตโนมัติ

🧾 Schema Review Checklist
- มี Primary Key ทุกตาราง
- มี Foreign Key ที่ถูกต้อง
- ใช้ชื่อ constraint ตามมาตรฐาน
- มี created_at, updated_at, deleted_at
- มี index ใน field สำคัญ
- ENUM ถูกเก็บใน lookup table
- ไม่มี TEXT สำหรับข้อมูล fix
- Migration file ชื่อถูกต้อง

✅ Summary
มาตรฐานนี้ช่วยให้:
- ทุก Feature มี Database Schema ที่สอดคล้องกัน
- Claude Architect Agent สามารถสร้าง SQL และ Model อัตโนมัติได้ถูกต้อง
- QA Agent ตรวจ schema consistency ระหว่าง API และ DB ได้อัตโนมัติ
- ลดปัญหาความไม่สอดคล้องของตาราง, FK, และ naming

📘 Reference Context
ใช้ร่วมกับ:
- core/conventions/naming-rules.md
- core/conventions/coding-style.md
- core/workflows/backend-database-subflow.md